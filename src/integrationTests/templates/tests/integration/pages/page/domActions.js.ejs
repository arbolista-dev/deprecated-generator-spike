import { By } from 'selenium-webdriver';
import until from 'selenium-webdriver/lib/until';
import { getDriver } from 'tests/integration/driver';

// See for driver docs https://seleniumhq.github.io/selenium/docs/api/javascript/
<% page.actions.forEach(action => { %>export async function <%= action %>(wait = 2500){

}
<% }); %>
