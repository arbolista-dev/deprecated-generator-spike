import * as pages from 'tests/integration/pages';

// use page actions and validations to assert test case behavior.
describe('<%= test.pageName %> <%= test.description %>', () => {
  beforeEach(async () => {
    await pages.<%= test.pageName %>.open();
  });

  <% test.assertions.forEach(assertion => { %>it('<%= assertion %>', async () => {
    // TODO
  });
  <% }); %>
});
